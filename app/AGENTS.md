# Project execution override

The design research and art direction are complete in `../DESIGN_CONTRACT.md`, and all production assets have already been created and visually inspected by the parent operator.

For this task:
- Do not run `ag design`, UI UX Pro Max search scripts, skill discovery, image inspection commands, or additional design research.
- Do not spend time reading global design skill files. Treat `../CLAUDE.md` and `../DESIGN_CONTRACT.md` as the final design source of truth.
- Begin implementation immediately by replacing `src/routes/index.tsx`, `src/styles.css`, and the metadata/public files.
- Use the exact prepared assets by filename. Their composition is already approved.
- Run the required test commands after implementation and fix failures.
